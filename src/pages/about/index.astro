---
/**
 * About me 페이지
 * 런타임에 Supabase about_data 테이블에서 데이터를 fetch한다.
 * AboutView(client:load) 컴포넌트가 실제 렌더링을 담당한다.
 */
import BaseLayout from "@/layouts/BaseLayout.astro";
import AboutView from "@/components/AboutView";
---

<BaseLayout title="About me - FoliumOnline" description="개발자 소개">
    <AboutView client:load />
</BaseLayout>
